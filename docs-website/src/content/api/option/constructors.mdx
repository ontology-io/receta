[**@ontology-io/receta**](../README.md)



# option/constructors

## Functions

### fromNullable()
 `>` **fromNullable**\<`T`\>(`value`): [`Option`](types.md#option)\<`T`\>

Defined in: [option/constructors/index.ts:55](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/option/constructors/index.ts#L55)

Converts a nullable value to an Option.

If the value is not null or undefined, returns Some with the value.
Otherwise, returns None.

#### Type Parameters

##### T

`T`

#### Parameters

##### value

The nullable value

`T` | `null` | `undefined`

#### Returns

[`Option`](types.md#option)\<`T`\>

Option containing the value or None

#### Example

```typescript
fromNullable(42) // => Some(42)
fromNullable(null) // => None
fromNullable(undefined) // => None

// With array find
const user = fromNullable(users.find(u => u.id === id))
```

### fromResult()
 `>` **fromResult**\<`T`, `E`\>(`result`): [`Option`](types.md#option)\<`T`\>

Defined in: [option/constructors/index.ts:76](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/option/constructors/index.ts#L76)

Converts a Result to an Option, discarding the error.

If the Result is Ok, returns Some with the value.
If the Result is Err, returns None.

#### Type Parameters

##### T

`T`

##### E

`E`

#### Parameters

##### result

[`Result`](../result/types.md#result)\<`T`, `E`\>

The Result to convert

#### Returns

[`Option`](types.md#option)\<`T`\>

Option containing the Ok value or None

#### Example

```typescript
// When you don't care about the specific error
const maybeNumber = fromResult(parseNumber(str))
// Ok(42) => Some(42)
// Err('Invalid') => None
```

### none()
 `>` **none**\<`T`\>(): [`None`](types.md#none)

Defined in: [option/constructors/index.ts:32](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/option/constructors/index.ts#L32)

Creates a None Option representing absence of a value.

#### Type Parameters

##### T

`T` `=` `never`

#### Returns

[`None`](types.md#none)

A None Option

#### Example

```typescript
const result = none()
// => { _tag: 'None' }
```

### some()
 `>` **some**\<`T`\>(`value`): [`Some`](types.md#some)\<`T`\>

Defined in: [option/constructors/index.ts:17](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/option/constructors/index.ts#L17)

Creates a Some Option containing a value.

#### Type Parameters

##### T

`T`

#### Parameters

##### value

`T`

The value to wrap

#### Returns

[`Some`](types.md#some)\<`T`\>

A Some Option containing the value

#### Example

```typescript
const result = some(42)
// => { _tag: 'Some', value: 42 }
```

### tryCatch()
 `>` **tryCatch**\<`T`\>(`fn`): [`Option`](types.md#option)\<`T`\>

Defined in: [option/constructors/index.ts:101](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/option/constructors/index.ts#L101)

Wraps a potentially throwing function in an Option.

If the function executes successfully, returns Some with the value.
If it throws, returns None.

#### Type Parameters

##### T

`T`

#### Parameters

##### fn

() => `T`

Function that may throw

#### Returns

[`Option`](types.md#option)\<`T`\>

Option containing either the return value or None

#### Example

```typescript
const parseJSON = (str: string) =>
  tryCatch(() => JSON.parse(str))

parseJSON('{"name":"John"}')
// => Some({ name: 'John' })

parseJSON('invalid json')
// => None
```
