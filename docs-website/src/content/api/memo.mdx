---
title: Memo
---

{/* AUTO-GENERATED from source code - Last updated: 2026-02-22 */}
{/* To update: modify JSDoc in src/memo/ and run: bun run docs:generate */}

[**@ontology-io/receta**](README.md)



# memo

## Functions

### memoize()
 `>` **memoize**\<`Args`, `R`\>(`fn`, `options?`): [`MemoizedFunction`](memo/types.md#memoizedfunction)\<`Args`, `R`\>

Defined in: [memo/memoize/index.ts:32](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/memo/memoize/index.ts#L32)

Creates a memoized version of a function that caches results based on arguments.

By default, uses a simple Map cache with the first argument as the key.
For multi-argument functions or complex keys, use `memoizeBy()` instead.

#### Type Parameters

##### Args

`Args` *extends* readonly \[`unknown`, `unknown`\]

##### R

`R`

#### Parameters

##### fn

(...`args`) => `R`

##### options?

[`MemoizeOptions`](memo/types.md#memoizeoptions) `=` `{}`

#### Returns

[`MemoizedFunction`](memo/types.md#memoizedfunction)\<`Args`, `R`\>

#### Example

```typescript
// Basic memoization
const fibonacci = (n: number): number => {
  if (n <= 1) return n
  return fibonacci(n - 1) + fibonacci(n - 2)
}

const memoFib = memoize(fibonacci)
memoFib(40) // computed (takes time)
memoFib(40) // cached (instant)

// With max size
const getUser = memoize(fetchUser, { maxSize: 100 })

// With TTL
const getWeather = memoize(fetchWeather, { ttl: 5 * 60 * 1000 }) // 5 min
```

#### See

 - memoizeBy - for custom key extraction
 - memoizeAsync - for async functions with deduplication

## References

### Cache

Re-exports [Cache](memo/types.md#cache)

### clearCache

Re-exports [clearCache](memo/clearCache.md#clearcache)

### invalidateAll

Re-exports [invalidateAll](memo/invalidation.md#invalidateall)

### invalidateMany

Re-exports [invalidateMany](memo/invalidation.md#invalidatemany)

### invalidateWhere

Re-exports [invalidateWhere](memo/invalidation.md#invalidatewhere)

### KeyFn

Re-exports [KeyFn](memo/types.md#keyfn)

### lruCache

Re-exports [lruCache](memo/caches/lruCache.md#lrucache)

### memoizeAsync

Re-exports [memoizeAsync](memo/memoizeAsync.md#memoizeasync)

### memoizeBy

Re-exports [memoizeBy](memo/memoizeBy.md#memoizeby)

### MemoizedAsyncFunction

Re-exports [MemoizedAsyncFunction](memo/types.md#memoizedasyncfunction)

### MemoizedFunction

Re-exports [MemoizedFunction](memo/types.md#memoizedfunction)

### MemoizeOptions

Re-exports [MemoizeOptions](memo/types.md#memoizeoptions)

### ttlCache

Re-exports [ttlCache](memo/caches/ttlCache.md#ttlcache)

### weakCache

Re-exports [weakCache](memo/caches/weakCache.md#weakcache)
