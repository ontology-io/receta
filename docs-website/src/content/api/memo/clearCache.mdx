[**@ontology-io/receta**](../README.md)



# memo/clearCache

## Functions

### clearCache()
 `>` **clearCache**\<`Args`, `R`\>(`memoized`): `void`

Defined in: [memo/clearCache/index.ts:23](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/memo/clearCache/index.ts#L23)

Clears the cache of a memoized function.

#### Type Parameters

##### Args

`Args` *extends* readonly `unknown`[]

##### R

`R`

#### Parameters

##### memoized

[`MemoizedFunction`](types.md#memoizedfunction)\<`Args`, `R`\> | [`MemoizedAsyncFunction`](types.md#memoizedasyncfunction)\<`Args`, `R`\>

#### Returns

`void`

#### Example

```typescript
import { memoize, clearCache } from 'receta/memo'

const expensive = memoize((n: number) => fibonacci(n))

expensive(40) // computed
expensive(40) // cached

clearCache(expensive)

expensive(40) // computed again

// Alternative: use .clear() method
expensive.clear()
```
