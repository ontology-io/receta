[**@ontology-io/receta**](../README.md)



# async/mapAsync

## Interfaces

### MapAsyncError

Defined in: [async/mapAsync/index.ts:7](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/async/mapAsync/index.ts#L7)

Error type for mapAsync failures.

#### Properties

##### error
 `>` `readonly` **error**: `unknown`

Defined in: [async/mapAsync/index.ts:11](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/async/mapAsync/index.ts#L11)

##### index
 `>` `readonly` **index**: `number`

Defined in: [async/mapAsync/index.ts:9](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/async/mapAsync/index.ts#L9)

##### item
 `>` `readonly` **item**: `unknown`

Defined in: [async/mapAsync/index.ts:10](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/async/mapAsync/index.ts#L10)

##### type
 `>` `readonly` **type**: `"map_async_error"`

Defined in: [async/mapAsync/index.ts:8](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/async/mapAsync/index.ts#L8)

## Variables

### mapAsyncOrThrow
 `>` `const` **mapAsyncOrThrow**: `never`

Defined in: [async/mapAsync/index.ts:201](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/async/mapAsync/index.ts#L201)

Throwing variant of mapAsync for backward compatibility.

Use this when you want exceptions instead of Result pattern.
Prefer the Result-returning mapAsync for better error handling.

This is implemented using the `orThrow` utility from Result module,
which converts any Result-returning function to a throwing variant.

#### Param

Array of items to map over

#### Param

Async function to apply to each item

#### Param

Concurrency options

#### Returns

Promise resolving to array of results

#### Throws

MapAsyncError if any mapper function fails

#### Example

```typescript
// Throws on error
try {
  const users = await mapAsyncOrThrow(
    userIds,
    async (id) => fetchUser(id),
    { concurrency: 5 }
  )
} catch (error) {
  console.error('Failed:', error)
}

// Equivalent to using orThrow directly
const mapAsyncOrThrow = orThrow(mapAsync)
```

#### See

 - mapAsync - Result-returning variant (recommended)
 - orThrow - utility for creating throwing variants

## Functions

### mapAsync()

#### Call Signature
 `>` **mapAsync**\<`T`, `U`\>(`items`, `fn`, `options?`): `Promise`\<[`Result`](../result/types.md#result)\<`U`[], [`MapAsyncError`](#mapasyncerror)\>\>

Defined in: [async/mapAsync/index.ts:64](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/async/mapAsync/index.ts#L64)

Maps over an array with an async function, with optional concurrency control.

Returns a Result to handle errors explicitly. If any mapper function fails,
the entire operation returns an Err with details about the failure.

This is more powerful than Promise.all(items.map(fn)) because it:
- Allows limiting concurrent operations (rate limiting, resource management)
- Returns Result for explicit error handling
- Provides detailed error information (which item failed)

##### Type Parameters

###### T

`T`

###### U

`U`

##### Parameters

###### items

readonly `T`[]

Array of items to map over

###### fn

(`item`, `index`) => `Promise`\<`U`\>

Async function to apply to each item

###### options?

[`ConcurrencyOptions`](types.md#concurrencyoptions)

Concurrency options

##### Returns

`Promise`\<[`Result`](../result/types.md#result)\<`U`[], [`MapAsyncError`](#mapasyncerror)\>\>

Promise resolving to Result containing array of results or error

##### Example

```typescript
import * as R from 'remeda'
import { unwrapOr, mapErr } from 'receta/result'

// Data-first: Fetch users with concurrency limit
const result = await mapAsync(
  ['user1', 'user2', 'user3'],
  async (id) => fetch(`/api/users/${id}`).then(r => r.json()),
  { concurrency: 2 }
)

// Handle with Result pattern
const users = R.pipe(
  result,
  mapErr(error => console.error('Failed at index:', error.index)),
  unwrapOr([])
)

// Data-last (in pipe)
const processed = await R.pipe(
  userIds,
  async (ids) => mapAsync(ids, fetchUser, { concurrency: 5 }),
  async (result) => unwrapOr(result, [])
)

// Unlimited concurrency (same as Promise.all but safer)
const all = await mapAsync(urls, fetchJSON)
```

##### See

 - filterAsync - for filtering with async predicates
 - parallel - for running tasks in parallel with concurrency limit
 - mapAsyncOrThrow - throwing variant for backward compatibility

#### Call Signature
 `>` **mapAsync**\<`T`, `U`\>(`fn`, `options?`): (`items`) => `Promise`\<[`Result`](../result/types.md#result)\<`U`[], [`MapAsyncError`](#mapasyncerror)\>\>

Defined in: [async/mapAsync/index.ts:70](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/async/mapAsync/index.ts#L70)

Maps over an array with an async function, with optional concurrency control.

Returns a Result to handle errors explicitly. If any mapper function fails,
the entire operation returns an Err with details about the failure.

This is more powerful than Promise.all(items.map(fn)) because it:
- Allows limiting concurrent operations (rate limiting, resource management)
- Returns Result for explicit error handling
- Provides detailed error information (which item failed)

##### Type Parameters

###### T

`T`

###### U

`U`

##### Parameters

###### fn

(`item`, `index`) => `Promise`\<`U`\>

Async function to apply to each item

###### options?

[`ConcurrencyOptions`](types.md#concurrencyoptions)

Concurrency options

##### Returns

Promise resolving to Result containing array of results or error
 `>` (`items`): `Promise`\<[`Result`](../result/types.md#result)\<`U`[], [`MapAsyncError`](#mapasyncerror)\>\>

###### Parameters

###### items

readonly `T`[]

###### Returns

`Promise`\<[`Result`](../result/types.md#result)\<`U`[], [`MapAsyncError`](#mapasyncerror)\>\>

##### Example

```typescript
import * as R from 'remeda'
import { unwrapOr, mapErr } from 'receta/result'

// Data-first: Fetch users with concurrency limit
const result = await mapAsync(
  ['user1', 'user2', 'user3'],
  async (id) => fetch(`/api/users/${id}`).then(r => r.json()),
  { concurrency: 2 }
)

// Handle with Result pattern
const users = R.pipe(
  result,
  mapErr(error => console.error('Failed at index:', error.index)),
  unwrapOr([])
)

// Data-last (in pipe)
const processed = await R.pipe(
  userIds,
  async (ids) => mapAsync(ids, fetchUser, { concurrency: 5 }),
  async (result) => unwrapOr(result, [])
)

// Unlimited concurrency (same as Promise.all but safer)
const all = await mapAsync(urls, fetchJSON)
```

##### See

 - filterAsync - for filtering with async predicates
 - parallel - for running tasks in parallel with concurrency limit
 - mapAsyncOrThrow - throwing variant for backward compatibility
