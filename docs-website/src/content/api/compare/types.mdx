[**@ontology-io/receta**](../README.md)



# compare/types

## Interfaces

### StringCompareOptions

Defined in: [compare/types.ts:22](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/compare/types.ts#L22)

Options for string comparison.

#### Properties

##### caseSensitive?
 `>` `readonly` `optional` **caseSensitive**: `boolean`

Defined in: [compare/types.ts:27](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/compare/types.ts#L27)

Whether comparison should be case-sensitive.

###### Default

```ts
true
```

##### locale?
 `>` `readonly` `optional` **locale**: `string`

Defined in: [compare/types.ts:33](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/compare/types.ts#L33)

Locale to use for comparison (e.g., 'en-US', 'fr-FR').

###### Default

```ts
undefined (uses default locale)
```

## Type Aliases

### ComparableExtractor()
 `>` **ComparableExtractor**\<`T`, `C`\> `=` (`value`) => `C`

Defined in: [compare/types.ts:44](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/compare/types.ts#L44)

Helper type for extracting a comparable value from T.

#### Type Parameters

##### T

`T`

##### C

`C` `=` `any`

#### Parameters

##### value

`T`

#### Returns

`C`

### Comparator()
 `>` **Comparator**\<`T`\> `=` (`a`, `b`) => `number`

Defined in: [compare/types.ts:17](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/compare/types.ts#L17)

A comparator function for sorting values of type T.

Returns:
- Negative number if a should come before b
- Zero if a and b are equal
- Positive number if a should come after b

#### Type Parameters

##### T

`T`

The type of values being compared

#### Parameters

##### a

`T`

##### b

`T`

#### Returns

`number`

#### Example

```typescript
const numericComparator: Comparator<number> = (a, b) => a - b
[3, 1, 2].sort(numericComparator) // => [1, 2, 3]
```

### Nullable
 `>` **Nullable**\<`T`\> `=` `T` \| `null` \| `undefined`

Defined in: [compare/types.ts:39](https://github.com/maxios/receta/blob/cc60b386f67b185c095dab8e7dc7a371fe636c2d/src/compare/types.ts#L39)

Represents a value that may be null or undefined.

#### Type Parameters

##### T

`T`
